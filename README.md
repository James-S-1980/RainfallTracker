# Rainfall Monitor

Run the app through Docker:

```powershell
docker compose up -d --build
```

Open:

```text
http://localhost:8768
```

Persistent runtime data is stored in the `rainfall-monitor-data` Docker volume.
